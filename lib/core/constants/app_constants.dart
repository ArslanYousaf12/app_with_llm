import 'package:flutter/material.dart';

class AppConstants {
  // Colors
  static const Color brightGreen = Color(0xFF9CFF00);
  static const Color darkGray = Color(0xFF2B2B2B);
  static const Color lightGray = Color(0xFF4A4A4A);
  static const Color textDark = Color(0xFF1A1A1A);
  static const Color textLight = Color(0xFFFFFFFF);
  static const Color backgroundDark = Color(0xFF1A1A1A);

  // Dimensions
  static const double buttonSize = 100.0;
  static const double buttonBorderWidth = 3.0;
  static const double timerDisplayFontSize = 140.0;
  static const double buttonTextSize = 18.0;
  static const double voiceIndicatorHeight = 60.0;
  static const double voiceIndicatorBorderRadius = 30.0;
  
  // Spacing
  static const double paddingSmall = 8.0;
  static const double paddingMedium = 16.0;
  static const double paddingLarge = 24.0;
  static const double paddingXLarge = 32.0;
  
  // Animation
  static const Duration animationDuration = Duration(milliseconds: 200);
}